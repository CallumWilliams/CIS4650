int gcd (int u, int v ) 
{
	if (v == 0) return u;
    else return gcd(v, u - u/v*v);    /* this is a comment */
      
    
}

void main (void) 
{
    /* 
    This is a another comment
    
     It stretches over multiple lines
     
     And shouldn't cause an issue
     
     */
	int x;
	int	y;
	x = input(); 
	y = input();
	output( gcd(x, y) );
}
