------------------------------------------
README
Authors: Matthew Goldman, Callum Williams
For: CIS4650, W17
------------------------------------------

Note:
 - the code is based off of the example 'java_tiny' provided on the
 Courselink.
 

Compiling:
1) Navigate to the primary directory of the project.
2) Type 'make' to compile the whole project, or:
	a) 'make Lexer.java', to compile the lexer.
	b) 'make parser.java', to compile the parser.
	c) 'make Main', to compile the source code.


Execution:
1) When in the primary directory, type:
	a) 'make test(1-5), to run tests 1-5.
	*for example, 'make test2' would run the parser on 
	cminus_samples/test2.cminus*
	b) java -classpath /usr/share/java/cup.jar:. Main "test-file",
	to test a custom file.
	
Limitations:

