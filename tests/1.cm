/* Test 1 - no errors */

int globe[3];

int functionOne(int c, int d) {
	
	int a;
	
	a = c*d;
	if (a == 0) return a;
	else return a + functionOne(c-1, d);
	
}

void functionTwo(int count) {

    while (count <= 10) {
	    count = count + 1;
	}
	return;
}

int main(void) {
	
	int x;
	x = functionOne(3, 2); 
	functionTwo(x);
	output(x);
	return x;
}
