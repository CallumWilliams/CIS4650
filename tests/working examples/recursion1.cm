/* Went through this manually and it works -- minus the jump */
void functionOne(int a, int b, int c)
{
    int x;
    x = a;
    x = b;
}

void main(void)
{   
    int z;
    int y;
    functionOne(z, 2 * y, 3 * 4);
}
