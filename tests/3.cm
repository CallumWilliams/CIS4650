/* Test 3-  no errors - recursion and If-statements */

int recursive(int a, int b)
{
    if(a > b)
    {
        output(a); /* Should print down from 10 to 3 */
        return recursive(a - 1, b);
    }
    return a;

}


int main(void) 
{
   output(recursive(10, 3)); 
}
